import { config } from "dotenv";
config({ path: ".env.local" });
(async () => {
  const { db } = await import("@/lib/db");
  const title = "【新機能】資料ライブラリ（OSの頭脳）— 媒体資料・他社資料に「聞く」と出典つきで答える";
  const body = `# 資料ライブラリ（OSの頭脳）

本部が登録した**媒体資料・提案書・他社資料**を、全員が検索し、質問すると**根拠の資料名とページ**を添えて答えます（NotebookLMのOS版）。
左メニュー「グループ共有」→ **資料ライブラリ**、または /dashboard/knowledge から開けます。

## 資料は2種類に分けて入っています

| 印 | 種類 | 使い方 |
|---|---|---|
| **自社** | アドアーチ自身の資料（TVer網羅プラン・パッケージ資料など） | 価格・実績・手順を**そのまま提案に使える** |
| **他社・媒体** | 媒体社・他社の資料 | 仕組み・仕様・配信面・条件は**参考にできる**。ただし**価格は卸値**（販売価格はTVerシミュレーター／パッケージ台帳で確認）、**実績は他社分**（自社実績として語らない） |

AIの答えも、この区別に従って「そのまま使える点」と「参考にとどめる点」を分けて書きます。

## 使い方

1. **資料に聞く**: 画面上部の枠に質問を入れて「聞く」。例「唐津市をTVerで網羅するといくら？」「この媒体の最小出稿額と入稿締切は？」「◯◯の資料で、うちの提案にそのまま使える仕組みはどれ？」
   - 答えの末尾に **出典 [1][2]**（資料名・ページ・引用文）が並びます。資料名を押すと全文へ
2. **資料を絞って聞く**: 一覧の行にチェックを入れてから聞くと、その資料だけを読んで答えます
3. **資料を読む**: 題名を押すと、要約・AIの整理（使える中身／価格の扱い／実績の扱い／注意）・全文・原本ダウンロード
4. **検索**: 題名・発行元・検索語・本文で探せます。「自社」「他社・媒体」で絞れます

## アーチくん・AI連携からも同じ資料が引けます

- 右下のアーチくん、みんなのチャットのアーチくんに聞くと、当たった資料の整理を踏まえて答えます
- お使いのClaude / ChatGPT（AI連携）では \`search_knowledge\`（検索）・\`list_knowledge\`（目次）・\`get_knowledge\`（全文）が使えます。例「資料ライブラリでTVerの配信面を調べて」

## 資料を入れてほしいとき

登録は本部が行います。媒体社からもらった資料・他社の提案書など「グループの頭脳に入れたい資料」があれば、本部（白川）にファイルを送ってください。
本部限定にした資料（加盟条件・原価表など）は、代表以外の一覧・検索・質問には出ません。
`;
  const tags = await db.wikiTag.findMany({ where: { name: { in: ["ヘルプガイド", "2026年9月更新", "営業支援"] } }, select: { id: true } });
  const exists = await db.wikiArticle.findFirst({ where: { title } });
  if (exists) { console.log("exists", exists.id); process.exit(0); }
  const a = await db.wikiArticle.create({ data: { title, body, authorName: "システム", branchId: "branch_hq", tags: { connect: tags.map((t) => ({ id: t.id })) } }, select: { id: true } });
  console.log("created", a.id, "tags", tags.length);
  process.exit(0);
})();
